---
import Layout from '../../../layouts/Layout.astro';
---

<Layout title="Week 9 Quiz: Arduino Digital I/O and Sensors">
    <main style="min-height: 100vh; background: linear-gradient(135deg, #fff3e0 0%, #ffcc80 100%); padding: 3rem 0;">
        <div style="max-width: 800px; margin: 0 auto; padding: 0 1rem;">
            <!-- Header -->
            <div style="text-align: center; margin-bottom: 2rem;">
                <div style="display: flex; align-items: center; justify-content: center; margin-bottom: 1rem;">
                    <div style="background: #f57c00; color: white; border-radius: 50%; width: 4rem; height: 4rem; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 1.5rem; margin-right: 1rem;">Q9</div>
                    <div style="text-align: left;">
                        <h1 style="font-size: 2rem; font-weight: bold; color: var(--black); margin: 0;">Week 9 Quiz</h1>
                        <p style="color: var(--gray); margin: 0;">Arduino Digital I/O and Sensors</p>
                    </div>
                </div>
            </div>

            <!-- Quiz Form -->
            <div style="background: white; border-radius: 12px; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); padding: 2rem;">
                <form id="week9Quiz" style="display: flex; flex-direction: column; gap: 2rem;">
                    <!-- Student Info -->
                    <div style="background: var(--gray-light); padding: 1.5rem; border-radius: 8px;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 1rem;">Student Information</h3>
                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1rem;">
                            <div>
                                <label style="display: block; font-weight: 500; color: var(--black); margin-bottom: 0.5rem;">Name:</label>
                                <input type="text" id="studentName" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 6px; font-size: 1rem;" required>
                            </div>
                            <div>
                                <label style="display: block; font-weight: 500; color: var(--black); margin-bottom: 0.5rem;">Email:</label>
                                <input type="email" id="studentEmail" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 6px; font-size: 1rem;" required>
                            </div>
                        </div>
                    </div>

                    <!-- Question 1 -->
                    <div style="border-left: 4px solid #f57c00; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 1: Multiple Choice (2 points)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">What function is used to configure a pin as an input or output in Arduino?</p>
                        <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q1" value="a" style="margin-right: 0.75rem;">
                                <span>A) digitalWrite()</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q1" value="b" style="margin-right: 0.75rem;">
                                <span>B) pinMode()</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q1" value="c" style="margin-right: 0.75rem;">
                                <span>C) digitalRead()</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q1" value="d" style="margin-right: 0.75rem;">
                                <span>D) analogWrite()</span>
                            </label>
                        </div>
                    </div>

                    <!-- Question 2 -->
                    <div style="border-left: 4px solid #f57c00; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 2: Multiple Choice (2 points)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">Which function reads the value from a digital pin?</p>
                        <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q2" value="a" style="margin-right: 0.75rem;">
                                <span>A) pinMode()</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q2" value="b" style="margin-right: 0.75rem;">
                                <span>B) digitalWrite()</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q2" value="c" style="margin-right: 0.75rem;">
                                <span>C) digitalRead()</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q2" value="d" style="margin-right: 0.75rem;">
                                <span>D) analogRead()</span>
                            </label>
                        </div>
                    </div>

                    <!-- Question 3 -->
                    <div style="border-left: 4px solid #f57c00; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 3: Short Answer (3 points)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">Write Arduino code to read a button connected to pin 2 and turn on an LED on pin 13 when the button is pressed:</p>
                        <textarea id="q3" name="q3" rows="6" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 6px; font-family: monospace; font-size: 0.9rem;" placeholder="Type your Arduino code here..."></textarea>
                    </div>

                    <!-- Question 4 -->
                    <div style="border-left: 4px solid #f57c00; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 4: True/False (2 points)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">A pull-up resistor is used to ensure a digital input pin reads HIGH when no button is pressed.</p>
                        <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q4" value="true" style="margin-right: 0.75rem;">
                                <span>True</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q4" value="false" style="margin-right: 0.75rem;">
                                <span>False</span>
                            </label>
                        </div>
                    </div>

                    <!-- Question 5 -->
                    <div style="border-left: 4px solid #f57c00; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 5: Multiple Choice (1 point)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">What does PWM stand for in Arduino programming?</p>
                        <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q5" value="a" style="margin-right: 0.75rem;">
                                <span>A) Power Width Modulation</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q5" value="b" style="margin-right: 0.75rem;">
                                <span>B) Pulse Width Modulation</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q5" value="c" style="margin-right: 0.75rem;">
                                <span>C) Pin Width Management</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q5" value="d" style="margin-right: 0.75rem;">
                                <span>D) Program Width Mode</span>
                            </label>
                        </div>
                    </div>

                    <!-- Submit Button -->
                    <div style="text-align: center; margin-top: 2rem;">
                        <button type="submit" style="background: #f57c00; color: white; padding: 1rem 2rem; border: none; border-radius: 8px; font-size: 1.1rem; font-weight: 600; cursor: pointer; transition: background-color 0.2s;">
                            Submit Quiz
                        </button>
                    </div>
                </form>

                <!-- Results Display -->
                <div id="quizResults" style="display: none; margin-top: 2rem; padding: 1.5rem; background: #fff3e0; border-radius: 8px;">
                    <h3 style="font-size: 1.25rem; font-weight: bold; color: #e65100; margin-bottom: 1rem;">Quiz Results</h3>
                    <div id="resultsContent"></div>
                </div>
            </div>

            <!-- Navigation -->
            <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 2rem;">
                <a href="/programming/lessons/week9" style="background: #f57c00; color: white; padding: 0.75rem 1.5rem; border-radius: 6px; text-decoration: none; transition: background-color 0.2s;">
                    ← Back to Lesson
                </a>
                <a href="/programming/semester1" style="background: var(--accent); color: white; padding: 0.75rem 1.5rem; border-radius: 6px; text-decoration: none; transition: background-color 0.2s;">
                    Semester Overview
                </a>
                <a href="/programming/lessons/week10" style="background: var(--accent-dark); color: white; padding: 0.75rem 1.5rem; border-radius: 6px; text-decoration: none; transition: background-color 0.2s;">
                    Next Lesson →
                </a>
            </div>
        </div>
    </main>

    <script is:inline>
window.onload = function() { generateCaptcha(); };
        // Quiz answers and scoring
        const correctAnswers = {
            q1: 'b', // pinMode()
            q2: 'c', // digitalRead()
            q4: 'true', // Pull-up resistor
            q5: 'b'  // Pulse Width Modulation
        };

        const questionPoints = {
            q1: 2,
            q2: 2,
            q3: 3,
            q4: 2,
            q5: 1
        };

        // Load saved email
        document.addEventListener('DOMContentLoaded', function() {
            const savedEmail = localStorage.getItem('studentEmail');
            if (savedEmail) {
                document.getElementById('studentEmail').value = savedEmail;
            }
        });

        document.getElementById('week9Quiz').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Save email to localStorage
            const email = document.getElementById('studentEmail').value;
            localStorage.setItem('studentEmail', email);
            
            // Calculate score
            let score = 0;
            let totalPoints = 10;
            let results = [];
            
            // Check multiple choice and true/false questions
            for (let qNum in correctAnswers) {
                const userAnswer = document.querySelector(`input[name="${qNum}"]:checked`)?.value;
                const isCorrect = userAnswer === correctAnswers[qNum];
                
                if (isCorrect) {
                    score += questionPoints[qNum];
                }
                
                results.push({
                    question: qNum,
                    userAnswer: userAnswer || 'No answer',
                    correctAnswer: correctAnswers[qNum],
                    isCorrect: isCorrect,
                    points: isCorrect ? questionPoints[qNum] : 0
                });
            }
            
            // Handle Question 3 (code writing) - give partial credit
            const q3Answer = document.getElementById('q3').value.toLowerCase();
            let q3Points = 0;
            if (q3Answer.includes('digitalread') && q3Answer.includes('digitalwrite') && q3Answer.includes('pinmode')) {
                q3Points = 3;
            } else if (q3Answer.includes('digitalread') && q3Answer.includes('digitalwrite')) {
                q3Points = 2;
            } else if (q3Answer.includes('digitalread') || q3Answer.includes('digitalwrite')) {
                q3Points = 1;
            }
            
            score += q3Points;
            results.push({
                question: 'q3',
                userAnswer: document.getElementById('q3').value || 'No answer',
                correctAnswer: 'Code should include pinMode(), digitalRead(), and digitalWrite()',
                isCorrect: q3Points === 3,
                points: q3Points
            });
            
            // Display results
            const percentage = Math.round((score / totalPoints) * 100);
            let grade = 'F';
            if (percentage >= 90) grade = 'A';
            else if (percentage >= 80) grade = 'B';
            else if (percentage >= 70) grade = 'C';
            else if (percentage >= 60) grade = 'D';
            
            const resultsHTML = `
                <div style="text-align: center; margin-bottom: 1rem;">
                    <div style="font-size: 2rem; font-weight: bold; color: #e65100;">${score}/${totalPoints} (${percentage}%)</div>
                    <div style="font-size: 1.5rem; font-weight: bold; color: #e65100;">Grade: ${grade}</div>
                </div>
                <div style="text-align: left;">
                    ${results.map(r => `
                        <div style="margin-bottom: 1rem; padding: 1rem; background: ${r.isCorrect ? '#e8f5e8' : '#ffeaea'}; border-radius: 6px;">
                            <strong>Question ${r.question.toUpperCase()}:</strong> ${r.isCorrect ? '✓' : '✗'} (${r.points} points)<br>
                            <strong>Your answer:</strong> ${r.userAnswer}<br>
                            <strong>Correct answer:</strong> ${r.correctAnswer}
                        </div>
                    `).join('')}
                </div>
            `;
            
            document.getElementById('resultsContent').innerHTML = resultsHTML;
            document.getElementById('quizResults').style.display = 'block';
            
            // Scroll to results
            document.getElementById('quizResults').scrollIntoView({ behavior: 'smooth' });
            
            // Send email notification
            const quizData = {
                studentName: document.getElementById('studentName').value,
                studentEmail: document.getElementById('studentEmail').value,
                week: 'Week 9',
                topic: 'Arduino Digital I/O and Sensors',
                score: score,
                totalPoints: totalPoints,
                percentage: percentage,
                grade: grade,
                results: results,
                submittedAt: new Date().toISOString()
            };
            
            // Submit to teacher via EmailJS (using Week 8's working format)
            emailjs.send('service_ot1jg6s', 'template_io02xtm', {
                to_email: 'chuck@c00lnerd.com',
                from_name: quizData.studentName,
                from_email: quizData.studentEmail,
                message: `WEEK 9 QUIZ RESULTS - DIGITAL I/O AND SENSORS

Student Information:
• Name: ${quizData.studentName}
• Email: ${quizData.studentEmail}

Quiz Performance:
• Score: ${quizData.score}/${quizData.totalPoints} (${quizData.percentage}%)
• Topic: ${quizData.topic}
• Submission Date: ${new Date(quizData.submittedAt).toLocaleString()}

Detailed Results:
${quizData.results.map(r => `• Question ${r.question.toUpperCase()}: ${r.userAnswer} ${r.isCorrect ? '✓' : '✗'} (${r.points} points)
  Correct Answer: ${r.correctAnswer}`).join('\n')}

Performance Analysis:
${quizData.percentage >= 80 ? '🎉 Excellent! Student demonstrates strong understanding of Arduino digital I/O and sensor concepts.' : 
  quizData.percentage >= 70 ? '👍 Good work! Student grasps digital I/O fundamentals but may need practice with circuit building.' :
  quizData.percentage >= 60 ? '⚠️ Satisfactory. Student understands basics but needs more practice with pinMode() and digitalWrite().' :
  '📚 Needs improvement. Recommend additional review of digital signals, pull-up resistors, and breadboard circuits.'}`,
                subject: `Week 9 Digital IO Quiz - ${quizData.studentName}`
            })
            .then(function(response) {
                console.log('Quiz results sent successfully!', response.status, response.text);
            }, function(error) {
                console.log('Failed to send quiz results...', error);
            });
        });
    </script>

    <!-- EmailJS SDK -->
    <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js">
window.onload = function() { generateCaptcha(); };</script>
    
    <script>
window.onload = function() { generateCaptcha(); };
        // Initialize EmailJS
        emailjs.init("ct_s19oZHppLy9BlW");
    // CAPTCHA variables
    let captchaAnswer = 0;
    
    function generateCaptcha() {
        const num1 = Math.floor(Math.random() * 10) + 1;
        const num2 = Math.floor(Math.random() * 10) + 1;
        captchaAnswer = num1 + num2;
        const questionElement = document.getElementById('captcha-question');
        if (questionElement) {
            questionElement.textContent = num1 + " + " + num2 + ;
        }
    }
    
    function validateCaptcha() {
        const userAnswer = parseInt(document.getElementById('captcha').value);
        const errorDiv = document.getElementById('captcha-error');
        
        if (userAnswer !== captchaAnswer) {
            if (errorDiv) errorDiv.style.display = 'block';
            generateCaptcha(); // Generate new question
            document.getElementById('captcha').value = '';
            return false;
        }
        
        if (errorDiv) errorDiv.style.display = 'none';
        return true;
    }
    
    // Security validation function
    function validateSecurity() {
        // Check honeypot field (should be empty)
        const honeypot = document.getElementById('website');
        if (honeypot && honeypot.value) {
            console.log('Spam bot detected via honeypot');
            alert('Security check failed. Please try again.');
            return false;
        }
        
        // Rate limiting check
        const lastSubmission = localStorage.getItem('lastQuizStart');
        const now = Date.now();
        if (lastSubmission && (now - parseInt(lastSubmission)) < 30000) { // 30 second cooldown
            alert('Please wait 30 seconds between quiz attempts.');
            return false;
        }
        localStorage.setItem('lastQuizStart', now.toString());
        
        // Validate CAPTCHA
        if (!validateCaptcha()) {
            return false;
        }
        
        // Email validation and domain filtering
        const emailInput = document.getElementById('email');
        if (emailInput && emailInput.value) {
            const email = emailInput.value;
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email)) {
                alert('Please enter a valid email address.');
                return false;
            }
            
            // Block common spam domains
            const spamDomains = ['tempmail.org', '10minutemail.com', 'guerrillamail.com', 'mailinator.com', 'throwaway.email'];
            const domain = email.split('@')[1].toLowerCase();
            if (spamDomains.includes(domain)) {
                alert('Please use a permanent email address.');
                return false;
            }
        }
        
        return true;
    }
    </script>
</Layout>

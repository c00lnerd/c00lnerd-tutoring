---
import Layout from '../../../layouts/Layout.astro';
---

<Layout title="Week 5 Quiz: Functions and Modular Programming">
    <main style="min-height: 100vh; background: linear-gradient(135deg, #fce7f3 0%, #fbcfe8 100%); padding: 3rem 0;">
        <div style="max-width: 800px; margin: 0 auto; padding: 0 1rem;">
            <!-- Header -->
            <div style="text-align: center; margin-bottom: 2rem;">
                <div style="display: flex; align-items: center; justify-content: center; margin-bottom: 1rem;">
                    <div style="background: #ec4899; color: white; border-radius: 50%; width: 4rem; height: 4rem; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 1.5rem; margin-right: 1rem;">Q5</div>
                    <div style="text-align: left;">
                        <h1 style="font-size: 2rem; font-weight: bold; color: var(--black); margin: 0;">Week 5 Quiz</h1>
                        <p style="color: var(--gray); margin: 0;">Functions and Modular Programming</p>
                    </div>
                </div>
            </div>

            <!-- Quiz Form -->
            <div style="background: white; border-radius: 12px; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); padding: 2rem;">
                <form id="week5Quiz" style="display: flex; flex-direction: column; gap: 2rem;">
                    <!-- Student Info -->
                    <div style="background: var(--gray-light); padding: 1.5rem; border-radius: 8px;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 1rem;">Student Information</h3>
                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1rem;">
                            <div>
                                <label style="display: block; font-weight: 500; color: var(--black); margin-bottom: 0.5rem;">Name:</label>
                                <input type="text" id="studentName" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 6px; font-size: 1rem;" required>
                            </div>
                            <div>
                                <label style="display: block; font-weight: 500; color: var(--black); margin-bottom: 0.5rem;">Email:</label>
                                <input type="email" id="studentEmail" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 6px; font-size: 1rem;" required>
                            </div>
                        </div>
                    </div>

                    <!-- Question 1 -->
                    <div style="border-left: 4px solid #ec4899; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 1: Multiple Choice (2 points)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">What keyword is used to define a function in Python?</p>
                        <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q1" value="a" style="margin-right: 0.75rem;">
                                <span>A) function</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q1" value="b" style="margin-right: 0.75rem;">
                                <span>B) def</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q1" value="c" style="margin-right: 0.75rem;">
                                <span>C) func</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q1" value="d" style="margin-right: 0.75rem;">
                                <span>D) define</span>
                            </label>
                        </div>
                    </div>

                    <!-- Question 2 -->
                    <div style="border-left: 4px solid #ec4899; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 2: Short Answer (3 points)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">Write a function called 'greet' that takes a name parameter and returns "Hello, [name]!":</p>
                        <textarea id="q2" name="q2" rows="4" style="width: 100%; padding: 0.75rem; border: 1px solid #d1d5db; border-radius: 6px; font-family: monospace; font-size: 0.9rem;" placeholder="Type your Python code here..."></textarea>
                    </div>

                    <!-- Question 3 -->
                    <div style="border-left: 4px solid #ec4899; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 3: Multiple Choice (2 points)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">What does the 'return' statement do in a function?</p>
                        <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q3" value="a" style="margin-right: 0.75rem;">
                                <span>A) Prints a value to the screen</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q3" value="b" style="margin-right: 0.75rem;">
                                <span>B) Sends a value back to the caller and exits the function</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q3" value="c" style="margin-right: 0.75rem;">
                                <span>C) Starts the function over</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q3" value="d" style="margin-right: 0.75rem;">
                                <span>D) Deletes the function</span>
                            </label>
                        </div>
                    </div>

                    <!-- Question 4 -->
                    <div style="border-left: 4px solid #ec4899; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 4: True/False (2 points)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">Functions can have multiple parameters separated by commas.</p>
                        <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q4" value="true" style="margin-right: 0.75rem;">
                                <span>True</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q4" value="false" style="margin-right: 0.75rem;">
                                <span>False</span>
                            </label>
                        </div>
                    </div>

                    <!-- Question 5 -->
                    <div style="border-left: 4px solid #ec4899; padding-left: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: var(--black); margin-bottom: 0.75rem;">Question 5: Multiple Choice (1 point)</h3>
                        <p style="color: var(--gray); margin-bottom: 1rem;">What happens if you call a function without providing all required parameters?</p>
                        <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q5" value="a" style="margin-right: 0.75rem;">
                                <span>A) The function runs with default values</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q5" value="b" style="margin-right: 0.75rem;">
                                <span>B) Python gives an error</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q5" value="c" style="margin-right: 0.75rem;">
                                <span>C) The function returns None</span>
                            </label>
                            <label style="display: flex; align-items: center;">
                                <input type="radio" name="q5" value="d" style="margin-right: 0.75rem;">
                                <span>D) Nothing happens</span>
                            </label>
                        </div>
                    </div>

                    <!-- Submit Button -->
                    <div style="text-align: center; margin-top: 2rem;">
                        <button type="submit" style="background: #ec4899; color: white; padding: 1rem 2rem; border: none; border-radius: 8px; font-size: 1.1rem; font-weight: 600; cursor: pointer; transition: background-color 0.2s;">
                            Submit Quiz
                        </button>
                    </div>
                </form>

                <!-- Results Display -->
                <div id="quizResults" style="display: none; margin-top: 2rem; padding: 1.5rem; background: #eff6ff; border-radius: 8px;">
                    <h3 style="font-size: 1.25rem; font-weight: bold; color: #1e40af; margin-bottom: 1rem;">Quiz Results</h3>
                    <div id="scoreDisplay" style="font-size: 1.1rem; margin-bottom: 1rem;"></div>
                    <div id="feedback" style="color: var(--gray);"></div>
                </div>
            </div>

            <!-- Navigation -->
            <div style="display: flex; flex-direction: column; gap: 1rem; margin-top: 2rem; align-items: center;">
                <a href="/programming/lessons/week5" style="
                    background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
                    color: white;
                    padding: 0.75rem 1.5rem;
                    border-radius: 8px;
                    text-decoration: none;
                    font-weight: 600;
                    min-width: 200px;
                    text-align: center;
                    transition: all 0.3s ease;
                    box-shadow: 0 2px 4px rgba(59, 130, 246, 0.3);
                " onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 4px 8px rgba(59, 130, 246, 0.4)'" onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 2px 4px rgba(59, 130, 246, 0.3)'">
                    ? Back to Lesson
                </a>
                <a href="/programming/semester1" style="
                    background: linear-gradient(135deg, #6b7280 0%, #4b5563 100%);
                    color: white;
                    padding: 0.75rem 1.5rem;
                    border-radius: 8px;
                    text-decoration: none;
                    font-weight: 600;
                    min-width: 200px;
                    text-align: center;
                    transition: all 0.3s ease;
                    box-shadow: 0 2px 4px rgba(107, 114, 128, 0.3);
                " onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 4px 8px rgba(107, 114, 128, 0.4)'" onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 2px 4px rgba(107, 114, 128, 0.3)'">
                    Semester Overview
                </a>
                <a href="/programming/lessons/week6" style="
                    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
                    color: white;
                    padding: 0.75rem 1.5rem;
                    border-radius: 8px;
                    text-decoration: none;
                    font-weight: 600;
                    min-width: 200px;
                    text-align: center;
                    transition: all 0.3s ease;
                    box-shadow: 0 2px 4px rgba(16, 185, 129, 0.3);
                " onmouseover="this.style.transform='translateY(-2px)'; this.style.boxShadow='0 4px 8px rgba(16, 185, 129, 0.4)'" onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='0 2px 4px rgba(16, 185, 129, 0.3)'">
                    Next Lesson ?
                </a>
            </div>
        </div>
    </main>

    <script is:inline>
window.onload = function() { generateCaptcha(); };
        // Quiz answers and scoring
        const correctAnswers = {
            q1: 'b',  // def
            q3: 'b',  // return sends value back and exits
            q4: 'true',  // functions can have multiple parameters
            q5: 'b'   // Python gives an error
        };

        const totalPoints = 10;

        document.getElementById('week5Quiz').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const studentName = document.getElementById('studentName').value;
            const studentEmail = document.getElementById('studentEmail').value;
            
            if (!studentName || !studentEmail) {
                alert('Please fill in your name and email.');
                return;
            }

            let score = 0;
            let feedback = [];

            // Check multiple choice answers
            Object.keys(correctAnswers).forEach(questionId => {
                const selectedAnswer = document.querySelector(`input[name="${questionId}"]:checked`);
                if (selectedAnswer && selectedAnswer.value === correctAnswers[questionId]) {
                    score += questionId === 'q1' || questionId === 'q3' || questionId === 'q4' ? 2 : 1;
                    feedback.push(`? Question ${questionId.slice(1)}: Correct`);
                } else {
                    feedback.push(`? Question ${questionId.slice(1)}: Incorrect`);
                }
            });

            // Check short answer (Q2) - basic validation
            const q2Answer = document.getElementById('q2').value.toLowerCase().trim();
            if (q2Answer.includes('def') && q2Answer.includes('greet') && q2Answer.includes('return')) {
                score += 3;
                feedback.push('? Question 2: Correct - Good function structure');
            } else {
                feedback.push('? Question 2: Missing key elements (def, function name, return statement)');
            }

            const percentage = Math.round((score / totalPoints) * 100);
            
            // Display results
            document.getElementById('scoreDisplay').innerHTML = `
                <strong>Score: ${score}/${totalPoints} (${percentage}%)</strong>
            `;
            
            document.getElementById('feedback').innerHTML = feedback.join('<br>');
            document.getElementById('quizResults').style.display = 'block';

            // Prepare quiz data for submission
            const quizData = {
                studentName: studentName,
                studentEmail: studentEmail,
                week: 5,
                topic: 'Functions and Modular Programming',
                score: score,
                totalPoints: totalPoints,
                percentage: percentage,
                answers: {
                    q1: document.querySelector('input[name="q1"]:checked')?.value || 'No answer',
                    q2: document.getElementById('q2').value || 'No answer',
                    q3: document.querySelector('input[name="q3"]:checked')?.value || 'No answer',
                    q4: document.querySelector('input[name="q4"]:checked')?.value || 'No answer',
                    q5: document.querySelector('input[name="q5"]:checked')?.value || 'No answer'
                },
                submittedAt: new Date().toISOString()
            };

            // Submit to teacher via EmailJS
            submitQuizResults(quizData);
        });

        function submitQuizResults(quizData) {
            const formattedMessage = `WEEK 5 QUIZ RESULTS - FUNCTIONS AND MODULAR PROGRAMMING

Student Information:
• Name: ${quizData.studentName}
• Email: ${quizData.studentEmail}

Quiz Performance:
• Score: ${quizData.score}/${quizData.totalPoints} (${quizData.percentage}%)
• Topic: ${quizData.topic}
• Submission Date: ${new Date(quizData.submittedAt).toLocaleString()}

Student Responses:
• Q1 (Function Definition): ${quizData.answers.q1} ${quizData.answers.q1 === 'b' ? '?' : '?'}
• Q2 (Function Code): ${quizData.answers.q2}
• Q3 (Return Statement): ${quizData.answers.q3} ${quizData.answers.q3 === 'b' ? '?' : '?'}
• Q4 (Multiple Parameters): ${quizData.answers.q4} ${quizData.answers.q4 === 'true' ? '?' : '?'}
• Q5 (Missing Parameters): ${quizData.answers.q5} ${quizData.answers.q5 === 'b' ? '?' : '?'}

Performance Analysis:
${quizData.percentage >= 80 ? '?? Excellent! Student shows strong understanding of functions and modular programming concepts.' : 
  quizData.percentage >= 70 ? '?? Good work! Student grasps function concepts but may need practice with syntax and structure.' :
  quizData.percentage >= 60 ? '?? Satisfactory. Student understands basics but needs more practice with function definition and usage.' :
  '?? Needs improvement. Recommend additional review of function syntax, parameters, and return statements.'}`;

            // Send email using the working inline format
            emailjs.send('service_kaw1xkh', 'template_io02xtm', {
                to_email: 'chuck@c00lnerd.com',
                from_name: quizData.studentName,
                from_email: quizData.studentEmail,
                message: formattedMessage,
                subject: `Week 5 Programming Quiz - ${quizData.studentName}`
            })
            .then(function(response) {
                console.log('Quiz results sent successfully!', response.status, response.text);
            }, function(error) {
                console.log('Failed to send quiz results...', error);
            });
        }

        // Initialize EmailJS
        emailjs.init("ct_s19oZHppLy9BlW");
    // CAPTCHA variables
    let captchaAnswer = 0;
    
    function generateCaptcha() {
        const num1 = Math.floor(Math.random() * 10) + 1;
        const num2 = Math.floor(Math.random() * 10) + 1;
        captchaAnswer = num1 + num2;
        const questionElement = document.getElementById('captcha-question');
        if (questionElement) {
            questionElement.textContent = num1 + " + " + num2;
        }
    }
    
    function validateCaptcha() {
        const userAnswer = parseInt(document.getElementById('captcha').value);
        const errorDiv = document.getElementById('captcha-error');
        
        if (userAnswer !== captchaAnswer) {
            if (errorDiv) errorDiv.style.display = 'block';
            generateCaptcha(); // Generate new question
            document.getElementById('captcha').value = '';
            return false;
        }
        
        if (errorDiv) errorDiv.style.display = 'none';
        return true;
    }
    
    // Security validation function
    function validateSecurity() {
        // Check honeypot field (should be empty)
        const honeypot = document.getElementById('website');
        if (honeypot && honeypot.value) {
            console.log('Spam bot detected via honeypot');
            alert('Security check failed. Please try again.');
            return false;
        }
        
        // Rate limiting check
        const lastSubmission = localStorage.getItem('lastQuizStart');
        const now = Date.now();
        if (lastSubmission && (now - parseInt(lastSubmission)) < 30000) { // 30 second cooldown
            alert('Please wait 30 seconds between quiz attempts.');
            return false;
        }
        localStorage.setItem('lastQuizStart', now.toString());
        
        // Validate CAPTCHA
        if (!validateCaptcha()) {
            return false;
        }
        
        // Email validation and domain filtering
        const emailInput = document.getElementById('email');
        if (emailInput && emailInput.value) {
            const email = emailInput.value;
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email)) {
                alert('Please enter a valid email address.');
                return false;
            }
            
            // Block common spam domains
            const spamDomains = ['tempmail.org', '10minutemail.com', 'guerrillamail.com', 'mailinator.com', 'throwaway.email'];
            const domain = email.split('@')[1].toLowerCase();
            if (spamDomains.includes(domain)) {
                alert('Please use a permanent email address.');
                return false;
            }
        }
        
        return true;
    }
    </script>
</Layout>
